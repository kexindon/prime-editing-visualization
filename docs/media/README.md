Screenshots and the walkthrough recording for [DOCUMENTATION.md](../../DOCUMENTATION.md).

The doc references these names; drop files in and uncomment the matching line:

    01-first-load.png     the app on load, empty
    02-sequence.png       step 1, sequence and reading frame
    03-pam.png            step 2, PAM hits with one selected
    04-edit.png           step 3, bases selected and replacement typed
    05-pegrna.png         step 4, the design drawn on the map
    06-bystanders.png     step 5, bystander options listed
    07-compare.png        step 6, RTT comparison
    walkthrough.gif       fallback if you do not upload an mp4 to GitHub

Video: GitHub strips <video> tags from Markdown. To get an inline player,
drag the .mp4 into a new issue comment (without submitting it) and paste the
user-images.githubusercontent.com URL GitHub returns. A GIF committed here
works too and needs no upload step, but is much larger and cannot be seeked.
